defaults:
  - defaults/model
  - defaults/train
  - defaults/encoder
  - defaults/decoder
  - _self_
  - settings: null

# The settings file is loaded last, so its values override
# both _self_ (this file) and the defaults above.
