# Slurm Resource Configuration
job_name: "encode-ls-v2-6.25"
time: "12:00:00"
gpu_mem: "full"
num_gpus: 1                 # Encoding uses one process/GPU with configurable batches.
ntasks: 1
cpus_per_task: 8
mem: "32G"
account: "def-ravanelm"

# Experiment Configuration
repo_path: "MelCausalVAE"
train_script: "scripts/encode_dataset.py"
extra_args: ""
hydra_settings:
  - "dicodec/encode-librispeech-dicodec-v2-ls-6.25.yaml"

# Sweep Configuration (optional)
sweep: null

dataset_name: "librispeech-aligned"
dataset_source: "scratch"
dataset_extension: "parquet"
dataset_path: "librispeech-aligned"
dataset_filter: ""
# Staging filter for copy_dataset.sh: keep only the partitions to copy/encode.
dataset_partitions:
  - train_clean_100
  - train_clean_360
  - train_other_500
  - dev_clean
  - dev_other
  - test_clean
  - test_other
venv_path: "/home/piermel/envs/vae"

# Notification
email: "pierfrancesco.melucci@gmail.com"
